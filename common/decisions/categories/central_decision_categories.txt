############################
##### Central America ######
############################

central_UFC_investment_category = {

	priority = 10
	icon = GFX_decision_category_generic_industry 
	
	allowed = {
	    OR = {
			original_tag =  PAN
			original_tag =  COS
			original_tag =  NIC
			original_tag =  HON
			original_tag =  ELS
			original_tag =  GUA
		}
    }

	available = { has_completed_focus = central_UFC_investment }
	
	visible = {
		has_completed_focus = central_UFC_investment
	}
}

central_communist_revolt_category = {	
	priority = 20
	icon = GFX_decision_category_generic_industry 

	allowed = {
	    OR = {
			original_tag =  PAN
			original_tag =  COS
			original_tag =  NIC
			original_tag =  HON
			original_tag =  ELS
			original_tag =  GUA
		}
		AND = {	
		    has_completed_focus = central_cooperate_companies
		    NOT = {
			    has_government = communism
		    }
	    }
		NOT = {
			has_completed_focus = central_subdue_peasants
		}
    }
	
	visible = {
	    AND = {	
		    has_completed_focus = central_cooperate_companies
		    NOT = {
			    has_government = communism
		    }
	    }
		NOT = {
			has_completed_focus = central_subdue_peasants
		}
	}	
}

central_american_intervention_category = {	
	priority = 20
	icon = GFX_decision_category_generic_industry 
	
	allowed = {
	    OR = {
			original_tag =  PAN
			original_tag =  COS
			original_tag =  NIC
			original_tag =  HON
			original_tag =  ELS
			original_tag =  GUA
		}
    }
	
	visible = {
		if = {
			limit = {
				original_tag = PAN 
			}
			check_variable = { PAN_central_american_concession_variable < 100 }
		}
		if = {
			limit = {
				original_tag = COS 
			}
			check_variable = { COS_central_american_concession_variable < 100 }
		}
		if = {
			limit = {
				original_tag = NIC 
			}
			check_variable = { NIC_central_american_concession_variable < 100 }
		}
		if = {
			limit = {
				original_tag = HON 
			}
			check_variable = { HON_central_american_concession_variable < 100 }
		}
		if = {
			limit = {
				original_tag = ELS 
			}
			check_variable = { ELS_central_american_concession_variable < 100 }
		}
		if = {
			limit = {
				original_tag = GUA 
			}
			check_variable = { GUA_central_american_concession_variable < 100 }
		}
		has_completed_focus = central_depose_companies
		NOT = {
			has_country_flag = central_american_concession_failed
		}
	}
}

